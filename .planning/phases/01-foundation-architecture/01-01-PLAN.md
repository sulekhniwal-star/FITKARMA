---
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/main_development.dart
  - lib/main_production.dart
  - lib/core/architecture/use_case.dart
  - lib/core/architecture/mapper.dart
  - packages/fitkarma_ui/pubspec.yaml
  - mason.yaml
  - config/dev.json
  - config/prod.json
autonomous: true
requirements:
  - CORE-01
---

# Plan 01-01: Foundation & Architecture (2025 Optimized)

Initialize the FitKarma Flutter project using **Dart Workspaces**, Riverpod 3.0, and a feature-first Clean Architecture.

## 1. Project Initialization & Workspace Setup
<tasks>
  <task id="01-01-01">
    <description>Initialize Flutter project and configure root as a Dart Workspace.</description>
    <automated>flutter create . --platforms android,ios --org com.fitkarma && echo "\nworkspace:\n  - .\n  - packages/fitkarma_ui" >> pubspec.yaml</automated>
  </task>
  <task id="01-01-02">
    <description>Add modern dependencies for Riverpod 3.0, Freezed, and Local Storage.</description>
    <automated>flutter pub add flutter_riverpod riverpod_annotation freezed_annotation json_annotation flutter_secure_storage mason_cli && flutter pub add --dev build_runner riverpod_generator freezed json_serializable custom_lint riverpod_lint</automated>
  </task>
</tasks>

## 2. Infrastructure & Local Packages
<tasks>
  <task id="01-01-03">
    <description>Setup `packages/fitkarma_ui` as a workspace-aware local package.</description>
    <automated>mkdir -p packages/fitkarma_ui/lib && echo "name: fitkarma_ui\ndescription: Design system for FitKarma.\nversion: 0.0.1\npublish_to: none\nenvironment:\n  sdk: '>=3.6.0 <4.0.0'\ndependencies:\n  flutter:\n    sdk: flutter" > packages/fitkarma_ui/pubspec.yaml</automated>
  </task>
  <task id="01-01-04">
    <description>Initialize Mason with custom 'feat' brick for Clean Architecture scaffolding.</description>
    <automated>mason init && mason add feat --git-url https://github.com/felangel/mason --git-path bricks/feature_brick</automated>
  </task>
</tasks>

## 3. Environment Config (dart-define-from-file)
<tasks>
  <task id="01-01-05">
    <description>Create environment config files for cleaner compilation commands.</description>
    <automated>mkdir -p config && echo '{\n  "APP_NAME": "FitKarma Dev",\n  "BASE_URL": "http://localhost:8090"\n}' > config/dev.json && echo '{\n  "APP_NAME": "FitKarma",\n  "BASE_URL": "https://api.fitkarma.app"\n}' > config/prod.json</automated>
  </task>
</tasks>

## 4. Clean Architecture Base & Entry Points
<tasks>
  <task id="01-01-06">
    <description>Establish Pure Dart base classes and Riverpod 3.0 entry points.</description>
    <automated>mkdir -p lib/core/architecture && echo "abstract class UseCase&lt;Type, Params&gt; {\n  Future&lt;Type&gt; call(Params params);\n}" > lib/core/architecture/use_case.dart && echo "abstract class Mapper&lt;Model, Entity&gt; {\n  Entity toEntity(Model model);\n  Model fromEntity(Entity entity);\n}" > lib/core/architecture/mapper.dart</automated>
  </task>
  <task id="01-01-07">
    <description>Create Flavor-aware entry points using `ProviderScope`.</description>
    <automated>echo "import 'package:flutter/material.dart';\nimport 'package:flutter_riverpod/flutter_riverpod.dart';\n\nvoid main() {\n  runApp(const ProviderScope(child: FitKarmaApp(flavor: 'Development')));\n}\n\nclass FitKarmaApp extends StatelessWidget {\n  final String flavor;\n  const FitKarmaApp({super.key, required this.flavor});\n  @override\n  Widget build(BuildContext context) => MaterialApp(home: Scaffold(body: Center(child: Text('FitKarma \$flavor'))));\n}" > lib/main_development.dart && cp lib/main_development.dart lib/main_production.dart && sed -i 's/Development/Production/g' lib/main_production.dart</automated>
  </task>
</tasks>

## Verification
- [ ] `flutter pub get` resolutions confirm workspace members.
- [ ] `config/dev.json` and `config/prod.json` are present.
- [ ] `packages/fitkarma_ui/pubspec.yaml` has SDK constraint 3.6.0+.
- [ ] Mason `feat` brick is correctly registered.

## must_haves
- Dart Workspaces enabled in root `pubspec.yaml`.
- SDK constraint >= 3.6.0.
- Centralized dependency injection via `ProviderScope`.
